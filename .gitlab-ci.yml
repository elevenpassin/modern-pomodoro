image: node

before_script:
  - npm install

cache:
  paths:
    - node_modules/
pages:
  stage: deploy
  script:
  - npm run build
  - mkdir .public
  - cp -r ./dist/**/* .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
